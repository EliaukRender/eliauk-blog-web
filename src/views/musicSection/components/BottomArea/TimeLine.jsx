import React, { memo } from 'react';
import { TimeLineStyles } from '@/views/musicSection/styles/TimeLine';

const TimeLine = (props) => {
	return <TimeLineStyles></TimeLineStyles>;
};

export default memo(TimeLine);
