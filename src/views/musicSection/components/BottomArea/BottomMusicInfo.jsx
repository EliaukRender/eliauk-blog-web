import React, { memo } from 'react';
import { BottomMusicInfoStyles } from '@/views/musicSection/styles/BottomMusicInfoStyles';

const BottomMusicInfo = () => {
	return <BottomMusicInfoStyles></BottomMusicInfoStyles>;
};

export default memo(BottomMusicInfo);
