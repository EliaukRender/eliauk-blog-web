import React, { memo } from 'react';
import { BottomOperationStyles } from '@/views/musicSection/styles/BottomOperationStyles';

const BottomOperation = (props) => {
	return <BottomOperationStyles></BottomOperationStyles>;
};

export default memo(BottomOperation);
