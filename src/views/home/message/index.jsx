import React, { memo } from 'react';
import { MessageSectionStyles } from '@/views/home/message/styles';

/**
 * @description: 板块---留言板
 */
const MessageSection = () => {
	return <MessageSectionStyles></MessageSectionStyles>;
};

export default memo(MessageSection);
