import React, { memo } from 'react';
import { MessageSectionWrapper } from '@/views/messageSection/styles';

const MessageSection = () => {
	return <MessageSectionWrapper></MessageSectionWrapper>;
};

export default memo(MessageSection);
