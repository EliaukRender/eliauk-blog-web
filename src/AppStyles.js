import styled from 'styled-components';

export const AppWrapper = styled.div`
	//min-width: 800px;

	.progress-bar {
		position: fixed;
		top: 0;
		left: 0;
		right: 0;
		height: 10px;
		background: var(--red);
		transform-origin: 0%;
	}
`;
